name: vigiroutes_terrain
description: VigiRoutes Terrain — Agent app for CT inspection centers
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  provider: ^6.1.2
  mobile_scanner: ^5.1.0
  intl: ^0.19.0
  cupertino_icons: ^1.0.6

flutter:
  uses-material-design: true
